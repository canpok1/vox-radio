- 日本語で回答してください。
- ユーザー確認が必要な場合は AskUserQuestion を使用してください。

## CLAUDE.mdの方針

- このファイルはできるだけシンプルに保つ
- 詳細なルールは `.claude/rules/` に分離する（パス固有ルールで必要時のみ読み込まれる）
- スキル固有の情報は `.claude/skills/` のスキル定義に記載する（スキル呼び出し時のみ読み込まれる）

## 実装の開始タイミング
- ユーザーから明示的に実装の指示がない限り、ファイルの編集・作成を行わないこと
- 調査・分析・タスク更新などの非実装作業中に、勝手に実装を始めないこと
- 実装を開始したい場合は、まずユーザーに確認を取ること

## 重要な判断の記録（ADR）

- アーキテクチャ・技術選定など重要度の高い判断をしたら、`create-adr` スキルの起動を検討すること（漏れなく記録するため）。
- 些末な実装詳細は対象外（ADRの冗長化を防ぐ）。

## タスク管理
- タスク・バックログの管理は Todoist で行う（詳細は `.claude/rules/task-management.md`）。GitHub Issue はタスク管理に使わない。

## GitHub PR・コメントの管理
- PRやコメントを作成する際は以下を守ること
  - 末尾にフッターを付与する
    ```
    ---
    🤖 Generated with [Claude Code](https://claude.ai/claude-code)
    ```
  - 対応する Todoist タスクがある場合は、PR本文にそのタスクへのリンクを記載すること
  - 作成内容に不備がないか作成後に確認すること（例: レイアウトが崩れている、フッターが付与されていないなど）
